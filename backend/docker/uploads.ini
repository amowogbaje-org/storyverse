; Raises the built-in PHP dev server's default upload limits (2M/8M) so the
; cover-image upload endpoint's 8MB validation rule can actually be reached -
; without this, PHP itself silently truncates/rejects the upload before
; Laravel ever sees it. See ImageUploadController::coverImage().
upload_max_filesize = 10M
post_max_size = 12M
